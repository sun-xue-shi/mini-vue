stages:
  - init
  - build
​
cache:
  key: ${CI_BUILD_REF_NAME}
  paths:
  - node_modules/
  - dist/
​
init:
  stage: init
  script:
  -  npm install
​
build:
  stage: build
  script:
  -  npm run build
